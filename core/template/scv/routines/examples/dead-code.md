---
name: dead-code
cadence: 1d
guardrails:
  - "공개 API·외부 진입점·리플렉션/동적 로딩 대상은 참조 0 이어도 제거 후보에서 제외"
  - "한 번의 제안은 리뷰 가능한 크기(파일 몇 개)로 제한"
  - "삭제는 반드시 PR 로 제안 — permanent 브랜치 직접 쓰기 금지"
  - "직전 실행에서 사용자가 거절한 제안을 반복하지 않는다"
exit:
  - "제거 제안 PR 이 열리면 종료"
  - "제거 후보가 없음을 확인하면 종료"
report: never
---

저장소에서 어디서도 참조되지 않는 코드 — 사용되지 않는 함수·export·파일 —
를 찾아, 왜 죽은 코드인지(참조 검색 결과)를 근거로 제거를 제안하는 PR 을 연다.
