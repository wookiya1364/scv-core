---
archived_at: 2026-08-21
archived_by: scv-core-syncbot
reason: TESTS ALL GATES OK — run-dry 972/972 in the scv-core layout and in a flattened single-copy payload layout (cp -RL, no root TEMPLATE_VERSION); tests/run.sh + core/tests/test-*.sh green. effort: standard, sequential. Accumulated regression: launched after push (test-only guard; every suite already ran in T3).
---

# Archive record

This plan was archived on 2026-08-21.

## Reason

- TESTS ALL GATES OK — run-dry 972/972 in the scv-core layout and in a flattened single-copy payload layout (cp -RL, no root TEMPLATE_VERSION); tests/run.sh + core/tests/test-*.sh green. effort: standard, sequential. Accumulated regression: launched after push (test-only guard; every suite already ran in T3).
