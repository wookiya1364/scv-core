---
archived_at: 2026-08-23
archived_by: wookiya1364
reason: "test-purity 8건 통과, 선언된 함수 13개 전부 약속을 지킴. 검사기 자체 확인이 첫 구현의 구멍(결정적 층에서 파일 읽기를 놓침)을 잡았다. run-dry 977 PASS/0 FAIL, core/tests 27개 실패 0."
---

# Archive record

This plan was archived on 2026-08-23.

## Reason

- 순수부/효과부 경계가 계약(`core/contracts/purity.md`)이 되고, 표식과 검사기로
  기계가 강제한다. `PROMOTE.md` 규칙에 들어가 다음 계획부터 자동 적용된다.
- **자체 확인이 실제로 값을 했다**: 첫 구현은 결정적 층에서 파일 읽기를 놓쳤고
  (`cat` 이 엄격 층에만 있었다), `--self-test` 가 그것을 잡았다.

## 이 규칙이 곧바로 잡은 것

저널 색인(첫 적용 사례)에서 두 건:
- 순수부가 소문자 변환에 `tr`, 필드 대입에 `printf -v` 를 쓰고 있었다.
  둘 다 셸 안에서 되는 일이라 외부 도구를 쓸 이유가 없었다.
- 표시 정규화가 가운데 공백까지 지워 `"de cision"` 을 통과시켰다. 문서는
  "앞뒤 공백만" 이라고 적혀 있었으니 코드가 틀렸다.

## 남은 일

- 기존 스크립트 대부분은 아직 표식이 없다. 새로 만드는 것부터 붙인다.
- `sed`/`awk`/`grep`/`jq` 에 파일 이름을 주는 경우는 못 잡는다 — 문서에 적혀 있다.
